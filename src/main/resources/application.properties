# 1. Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/crave_feed_db
spring.datasource.username=postgres
spring.datasource.password=860540

# 2. JPA / Hibernate Settings (The Magic)
# This line automatically creates tables based on your Java code
spring.jpa.hibernate.ddl-auto=update

# Show SQL in the console (So we can see what's happening)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect